# Copy to .env and fill in. The dev-server reads these via tsx --env-file=.env.

# Required for /api/ai (Anthropic via Vercel AI SDK)
ANTHROPIC_API_KEY=sk-ant-...

# Optional — defaults to 3001
# PORT=3001
